#include "cub3d.h"

int main(int argc, char **argv)
{

	void	*mlx;
	void	*mlx_win;
    if (mlx)
    {}
    else if (argc)
    {}
    else if (argv)
    {}
    else if (mlx_win)
    {}

	mlx = mlx_init();
	mlx_win = mlx_new_window(mlx, 1920, 1080, "Hello world!");
	mlx_loop(mlx);
}